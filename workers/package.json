{
  "name": "mcp-memory-gateway-worker",
  "version": "0.9.0",
  "description": "Cloudflare Workers MCP server for mcp-memory-gateway Pro tier ($19/mo)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run typecheck && tsx --test src/**/*.test.ts",
    "types": "wrangler types"
  },
  "dependencies": {
    "stripe": "^20.4.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "@cloudflare/workers-types": "^4.20260313.1",
    "tsx": "^4.20.6",
    "typescript": "^5.5.0"
  }
}
