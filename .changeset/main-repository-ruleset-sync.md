---
"thumbgate": patch
---

feat(ops): sync active zero-bypass `main governance` repository ruleset (layered with classic branch protection) and restore required merge-quality checks including GitGuardian
