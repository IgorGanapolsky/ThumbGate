---
"thumbgate": patch
---

Add package-manager-honesty-doctor: lockfile/CI parity + fail-closed manager switches (InfoQ pnpm 12 process steal). Stays on npm; does not migrate to pnpm. Adds require-package-manager-lockfile-ci-parity and checkpoint-package-manager-switch gates.
