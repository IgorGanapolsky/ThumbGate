---
'thumbgate': patch
---

Route `main`-targeting PR manager merges through `/trunk merge` comments so autonomous merge requests work under Enterprise Managed User accounts without falling back to blocked GraphQL merge mutations.
