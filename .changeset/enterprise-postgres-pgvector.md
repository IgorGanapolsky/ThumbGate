---
"thumbgate": patch
---

Add Team/Enterprise Postgres + pgvector storage. Ships tenant-scoped schema generation, guarded setup/migration CLI commands, local JSONL import SQL, pgvector/HNSW indexes, row-level-security policies, and a storage adapter path selected by `THUMBGATE_STORAGE=postgres`.
