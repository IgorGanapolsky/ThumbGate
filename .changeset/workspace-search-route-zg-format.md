---
"thumbgate": patch
---

Steal zg (zvec-grep) four-route local search FORMAT onto existing rails via `workspace-search-route` doctor — ripgrep / BM25 / vector / hybrid RRF / Graphify — without installing `@zvec/zvec-grep`.
