---
'thumbgate': patch
---

Dedicated YouTube/CPC landing at `/yt` (alias `/aias-registration-yt`) maps six business-function agents onto existing gates. FORMAT steal from AIAS registration landings — not affiliated, not a 6-agent OS, no $499 hero. Bundle cap 511 → 512 for `public/yt.html`. YouTube CTAs hop through `/go/github`, `/go/npm`, and `/go/marketplace` so first-party click telemetry records before the off-site redirect.
