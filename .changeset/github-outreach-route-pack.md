---
'thumbgate': patch
---

Add route-aware GitHub outreach exports so the revenue loop now emits preferred contact paths plus CSV and JSON operator queues alongside the checked-in outreach markdown.
