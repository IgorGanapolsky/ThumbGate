---
"thumbgate": minor
---

feat(cli): add `thumbgate inventory` — read-only rollup of agent tool calls, gate denies by gate, per-day activity, and a false-deny rate that reports null with a stated reason rather than inventing a denominator.
