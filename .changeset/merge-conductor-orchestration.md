---
'thumbgate': patch
---

Hardened PR orchestration by aligning `pr-manager` with Trunk-managed `main` merges, suppressing duplicate queue submissions, and prioritizing release and workflow lanes ahead of dependency and feature traffic.
