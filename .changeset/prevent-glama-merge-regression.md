---
"thumbgate": patch
---

Add prove:glama-mcp start-contract pin and self-heal check so Glama registry install regressions (missing serve args, npm start guess, expanded MCP profile default) fail closed before merge.
