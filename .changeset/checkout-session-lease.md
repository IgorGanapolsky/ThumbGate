---
'thumbgate': patch
---

fix(hygiene): single-writer checkout session lease prevents concurrent-agent collisions (git clean / git add -A sweeps); pre-commit refuses commits under live foreign lease
