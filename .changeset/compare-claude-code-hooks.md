---
"thumbgate": patch
---

site: add /compare/claude-code-hooks — ThumbGate vs native Claude Code hooks and free community repos

New buyer-intent comparison page targeting the query developers actually run before adopting ThumbGate: "do I need this if Claude Code already has hooks?". Fairly credits native PreToolUse hooks and disler/claude-code-hooks-mastery (free, effective for static patterns), then draws the honest line — static hooks are config you maintain by hand with no cross-session learning, whereas ThumbGate runs in the same hook and auto-promotes a thumbs-down into a prevention rule across sessions and agents. No traction or benchmark claims (pre-revenue). Linked from the landing-page compare section for internal-link equity.
