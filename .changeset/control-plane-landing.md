---
"thumbgate": patch
---

Add a `/control-plane` marketing landing page (with `/agent-control-plane` and `/sessions` aliases) for the planned reliability/safety control-plane product. Captures waitlist signups via the existing `/api/newsletter` endpoint and routes the primary CTA to the live `$499` diagnostic. Page honestly differentiates against CloudCLI (session browser) and does NOT ship multi-tenant hosted dashboard code — that waits for the Phase-0 demand gate.
