---
"thumbgate": minor
---

Replace the `👍👎` emoji header logo with a proper teal-on-navy SVG brand mark across every customer-facing surface (landing page, dashboard, lessons, Pro, Learn hub, Learn articles, SEO-GSD generated pages, and the post-checkout Context Gateway Activated page). Ships `public/assets/brand/thumbgate-mark.svg`, `public/favicon.svg`, and `public/og.png`; wires `rel="icon"`, `apple-touch-icon`, and `og:image` tags on the main pages so tab icons and link previews render the brand consistently instead of OS-dependent Unicode glyphs. Hero-thumbs decorative art on the landing page is preserved intentionally.
