---
'thumbgate': minor
---

feat(egress): CrabTrap-inspired two-tier agent egress policy — static rules (deny wins), SSRF private-range block, observe→draft policy, replay eval, injection-safe judge view, Bash helper
