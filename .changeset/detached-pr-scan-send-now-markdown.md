---
'thumbgate': patch
---

Recover detached-head PR scans so repo-wide review triage still works in automation worktrees, and export an operator-ready send-now Markdown queue alongside the GTM revenue loop CSV and JSON artifacts.
