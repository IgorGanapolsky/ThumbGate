---
'thumbgate': patch
---

Harden the autonomous GTM revenue loop so hosted billing probes and GitHub discovery requests time out instead of hanging, honor `GITHUB_PERSONAL_ACCESS_TOKEN` as a fallback auth source, and refresh the checked-in operator sales assets from the newly unblocked live revenue run.
