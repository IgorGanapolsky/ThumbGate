---
"thumbgate": patch
---

Retarget npm `keywords` and `description` to buyer-intent search terms. Discovery is the verified bottleneck — ThumbGate is invisible on the queries buyers run ("claude code guardrails", "block dangerous tool calls", "ai agent firewall") while the package metadata was filled with internal jargon (`dpo`, `thompson-sampling`, `memalign`, `agentic-commerce`, `prevent-ai-slop`) that no buyer searches. Description now leads with the value prop; keywords cover the agent clients and the actual problem terms.
