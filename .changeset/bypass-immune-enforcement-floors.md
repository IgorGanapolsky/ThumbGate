---
"thumbgate": patch
---

Keep secret exfiltration, critical security-scan findings, and all four self-protection gate classes as unconditional hard floors even when the operator bypass is active. Ordinary block gates remain advisory by default and retain explicit strict-mode enforcement.
