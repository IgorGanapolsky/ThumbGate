---
"thumbgate": patch
---

Hard-block agent-initiated spend/upgrade by default (Apollo $588 incident): never-spend gates are hard-floor denials (not warn-by-default), free tier always loads permanent force-promote blocks, pattern matching covers WebFetch URLs, block gates prefer over warn, and ship thumbgate-spend-guard for PreToolUse hard deny.
