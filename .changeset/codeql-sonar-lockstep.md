---
"thumbgate": patch
---

Lock CodeQL init/analyze action pins to the same SHA, bump Sonar quality-gate action to v1.2.1, group Dependabot codeql-action updates, and add regression tests so half-bumps cannot re-break CI.
