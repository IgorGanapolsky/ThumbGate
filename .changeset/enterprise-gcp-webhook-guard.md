---
"thumbgate": minor
---

Add the first enterprise GCP guardrail slice: `thumbgate enterprise-gcp-webhook`.

The command evaluates Dialogflow CX webhook fulfillment requests before side effects, returns allow/review/block decisions, supports Dialogflow CX response envelopes with `--response`, and documents the feature as a scoped enterprise pilot rather than a fully native CCAI integration.
